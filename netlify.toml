[build]
  command = "npm run build"
  publish = "dist" # Adjust to match your Vite build output folder
  functions = "netlify/functions"

[dev]
  command = "npm run dev"
  port = 8888
  functionsPort = 9999

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
